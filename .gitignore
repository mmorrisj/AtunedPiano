__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.coverage
htmlcov/

# Reference recordings are kept out of git by default -- they are large and personal to
# one piano. Keep the protocol and the notes file, not the audio.
data/reference/*.wav
data/reference/*.flac
data/reference/*.aiff
